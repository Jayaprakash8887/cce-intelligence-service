<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <springProfile name="!json">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - [%X{correlationId}] [%X{subject}] [%X{intelligenceEventId}] [%X{intelligenceDeliveryId}] [%X{protocolCanonical}] %msg%n</pattern>
            </encoder>
        </appender>
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

    <springProfile name="json">
        <appender name="CONSOLE_JSON" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="net.logstash.logback.encoder.LogstashEncoder">
                <includeMdcKeyName>correlationId</includeMdcKeyName>
                <includeMdcKeyName>subject</includeMdcKeyName>
                <includeMdcKeyName>intelligenceEventId</includeMdcKeyName>
                <includeMdcKeyName>intelligenceDeliveryId</includeMdcKeyName>
                <includeMdcKeyName>protocolCanonical</includeMdcKeyName>
                <includeMdcKeyName>stepState</includeMdcKeyName>
            </encoder>
        </appender>
        <root level="INFO">
            <appender-ref ref="CONSOLE_JSON"/>
        </root>
    </springProfile>

    <logger name="org.openphc.cce.intelligence" level="INFO"/>
    <logger name="org.openphc.cce.intelligence.engine.FhirPayloadBuilder" level="DEBUG"/>
    <logger name="org.openphc.cce.intelligence.webhook" level="INFO"/>
    <logger name="org.springframework.kafka" level="WARN"/>
</configuration>
